<?xml version="1.0" encoding="utf-8"?>
<!-- DragonX: one numbered cell of the 24-word restore grid. Each cell is independent, so a single
     word can be corrected in place without shifting the others. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingStart="3dp"
    android:paddingEnd="3dp"
    android:paddingTop="2dp"
    android:paddingBottom="2dp">

    <TextView
        android:id="@+id/text_number"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textColor="@color/text_light_dimmed"
        android:textSize="10sp"
        tools:text="1" />

    <AutoCompleteTextView
        android:id="@+id/input_word"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/zcashBlack_54"
        android:completionThreshold="1"
        android:imeOptions="actionNext"
        android:inputType="textVisiblePassword|textNoSuggestions"
        android:maxLines="1"
        android:paddingStart="6dp"
        android:paddingEnd="2dp"
        android:paddingTop="6dp"
        android:paddingBottom="6dp"
        android:singleLine="true"
        android:textColor="@color/text_light"
        android:textColorHint="#757575"
        android:textSize="13sp" />
</LinearLayout>
